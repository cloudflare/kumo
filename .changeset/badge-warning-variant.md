---
"@cloudflare/kumo": minor
---

`Badge`: add `warning` variant for caution state indicators

Uses `bg-kumo-warning text-white`, consistent with `destructive` and `success` solid-background variants and the `kumo-warning` token family from `Banner`.

```tsx
<Badge variant="warning">Caution</Badge>
```
