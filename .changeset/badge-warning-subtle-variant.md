---
"@cloudflare/kumo": minor
---

`Badge`: add `warning-subtle` variant with low-opacity background and full-color text

Uses `bg-kumo-warning/10 text-kumo-warning`, consistent with the subtle pattern established by `Banner`'s alert variant.

```tsx
<Badge variant="warning-subtle">Caution</Badge>
```
