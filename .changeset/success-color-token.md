---
"@cloudflare/kumo": patch
---

Existing success colour badge didn't meet a11y contrast requirement and has been updated to use new colour swatches.
- Dark mode now uses the same token as light mode to ensure consistent contrast ratios.
- `Success` text is now using text-kumo-success instead of text-kumo-link
- `Badges` are updated to use the new success colour token